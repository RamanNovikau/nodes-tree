import React from 'react';
import {
  RenameNodeModalView,
  RenameNodeModalViewProps,
} from './RenameNodeModalView.tsx';

export const RenameNodeModal: React.FC<RenameNodeModalViewProps> = (props) => {
  return <RenameNodeModalView {...props} />;
};
